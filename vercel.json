{
  "buildCommand": "npm install && bash -c 'cd client && npm install && npx vite build'",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Build includes install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
