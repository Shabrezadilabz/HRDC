{
  "buildCommand": "npm install && npx vite build client",
  "outputDirectory": "client/dist",
  "installCommand": "echo 'Build includes install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
